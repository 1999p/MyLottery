<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xyr.lottery.infrastructure.dao.IActivityDao">

    <!--自定义映射，解决字段名和实体类中的属性名不一致问题-->
    <resultMap id="activityMap" type="cn.xyr.lottery.infrastructure.po.Activity">
        <id column="id" property="id"></id>
        <result column="activity_id" property="activityId"></result>
        <result column="activity_name" property="activityName"></result>
        <result column="activity_desc" property="activityDesc"></result>
        <result column="begin_date_time" property="beginDateTime"/>
        <result column="end_date_time" property="endDateTime"/>
        <result column="stock_count" property="stockCount"/>
        <result column="take_count" property="takeCount"/>
        <result column="state" property="state"/>
        <result column="creator" property="creator"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

    </resultMap>
    <insert id="insert" parameterType="activity">
        insert into activity (activityId, activityName, activityDesc, beginDateTime, endDateTime, stockCount, takeCount,
                              state, creator, createTime, updateTime)
        values (#{activityId}, #{activityName}, #{activityDesc}, #{beginDateTime}, #{endDateTime},
                #{stockCount}, #{takeCount}, #{state}, #{creator}, now(), now());
    </insert>
    <select id="queryActivityById" parameterType="java.lang.Long" resultMap="activityMap">
        select activityId,
               activityName,
               activityDesc,
               beginDateTime,
               endDateTime,
               stockCount,
               takeCount,
               state,
               creator,
               createTime,
               updateTime
        from activity
        where activityId = #{activityId}
    </select>


</mapper>