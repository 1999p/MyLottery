<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.xyr.lottery.infrastructure.dao.IStrategyDao">

    <select id="queryStrategy" resultType="cn.xyr.lottery.infrastructure.po.Strategy">
        select id,
               strategyId,
               strategyDesc,
               strategyMode,
               grantType,
               grantDate,
               extInfo,
               createTime,
               updateTime
        from strategy
        where strategyId = #{strategyId};
    </select>
</mapper>